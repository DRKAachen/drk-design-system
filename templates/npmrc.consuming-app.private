# Optional: copy into consuming app repo as .npmrc when using private GitHub Packages.
@drkaachen:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
always-auth=true
